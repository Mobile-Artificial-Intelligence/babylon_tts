# Run with `dart run ffigen --config ffigen.yaml`.
name: BabylonTtsBindings
description: |
  Bindings for `src/babylon_tts.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/babylon_tts_bindings_generated.dart'
headers:
  entry-points:
    - 'src/babylon_tts.h'
  include-directives:
    - 'src/babylon_tts.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
